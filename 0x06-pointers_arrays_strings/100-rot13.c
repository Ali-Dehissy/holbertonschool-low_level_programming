#include "main.h"
#include <stdio.h>
/**
 * *rot13 - encodes to rot 13
 *@a : pointer
 * Return: Always 0.
 */
char *rot13(char *a)
{
	int i;
	int j;
	char *rot = "nopqrstuvwxyzabcdefghijklm";

	for (i = 0; a[i] = '\0'; i++)
	{
	for (j = 0; j < 26; j++)
	if (a[i] == rot[j])
	{

	}
	}
	return (a);
}

