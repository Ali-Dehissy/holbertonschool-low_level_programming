#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - returns length of string
 * @s : pointer
 * Return: Always 0.
 */
int _strlen(char *s)
{
	char c;

	c = strlen(s);
	{
	_putchar(c);
	}
}
