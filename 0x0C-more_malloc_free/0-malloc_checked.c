#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
/**
 * *malloc checked - malloc check
 *@b : pointer
 * Return: Always 0.
 */
void *malloc_checked(unsigned int b)
{
	int *r;

	r = malloc(b);
	if (a == NULL)
	exit(98);
}
