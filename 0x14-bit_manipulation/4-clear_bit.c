#include "main.h"
/**
 *clear_bit - clear a bite
 *@n : unisgned long integer
 *@index : unsigned integer
 *Return: index
 */
int clear_bit(unsigned long int *n, unsigned int index)
{
	int clear_bit(unsigned long int *n, unsigned int index)

	if (index > (sizeof(unsigned long int) * 8))

		return (-1);

	n &= ~(1 << x);

	return (1);
}
