#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - returns length of string
 * @s : pointer
 * Return: Always 0.
 */
int _strlen(char *s)
{
	char length;

	length = strlen(s);
	
	return (length); 
}
